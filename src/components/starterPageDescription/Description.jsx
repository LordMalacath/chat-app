import "./style.scss"

//----------------------------------------------------------------

export default function Description({content}) {
    return <span className='description'>{content}</span>
};
